'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';

type User = { name?: string | null; email?: string | null; creditBalance: number; creditsUsed: number; creditsPurchased: number; creditsGranted: number; isPremium: boolean; premiumTier?: string | null; dailyGenerationCount: number };
export default function DashboardPage() { const [user, setUser] = useState<User | null>(null); const [error, setError] = useState(''); useEffect(() => { fetch('/api/me').then(async r => { const d = await r.json(); if (!r.ok) setError(d.error); else setUser(d.user); }); }, []); if (error) return <main className="min-h-screen bg-slate-950 p-8 text-white">{error}</main>; if (!user) return <main className="min-h-screen bg-slate-950 p-8 text-white">Chargement…</main>; return <main className="min-h-screen bg-slate-950 px-4 py-10 text-white md:px-8"><div className="mx-auto max-w-7xl"><header className="mb-8 flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm uppercase tracking-[.2em] text-pink-300">Tableau de bord</p><h1 className="mt-2 text-3xl font-black">Bonjour {user.name || user.email}</h1><p className="mt-2 text-slate-400">{user.isPremium ? `Abonnement ${user.premiumTier}` : 'Compte gratuit'}</p></div><div className="flex gap-3"><Link href="/pricing" className="rounded-full border border-slate-700 px-4 py-2">Acheter des crédits</Link><Link href="/generator" className="rounded-full bg-pink-500 px-4 py-2 font-semibold">Nouvelle réponse</Link></div></header><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{[['Solde', user.creditBalance], ['Crédits utilisés', user.creditsUsed], ['Crédits achetés', user.creditsPurchased], ['Générations aujourd’hui', user.dailyGenerationCount]].map(([label, value]) => <div key={label} className="rounded-3xl border border-slate-800 bg-slate-900 p-6"><p className="text-sm text-slate-400">{label}</p><p className="mt-3 text-3xl font-black">{value}</p></div>)}</div><section className="mt-8 rounded-3xl border border-slate-800 bg-slate-900 p-6"><div className="flex items-center justify-between"><h2 className="text-xl font-bold">Votre espace</h2><Link href="/history" className="text-sm text-pink-300">Voir l’historique →</Link></div><p className="mt-4 text-slate-300">Crédits offerts : {user.creditsGranted} · Statut : {user.isPremium ? 'Premium' : 'Gratuit'}</p></section></div></main>; }
